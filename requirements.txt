# Core dependencies
torch>=2.0.0
numpy>=1.17
datasets>=4.8.0

# HuggingFace stack (pinned minimums to avoid CommitInfo / httpx breakage)
huggingface_hub>=1.14.0
httpx>=0.28.0

# Optional: experiment tracking
# wandb
